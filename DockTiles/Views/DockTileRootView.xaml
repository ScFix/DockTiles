<UserControl x:Class="DockTiles.Views.DockTileRootView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DockTiles.Views"
             xmlns:vm="clr-namespace:DockTiles.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300">
    <UserControl.DataContext>
        <vm:DockTileManagerViewModel />
    </UserControl.DataContext>
    <Grid>
        <ContentPresenter Content="{Binding TreeRoot}"/>
    </Grid>
</UserControl>
